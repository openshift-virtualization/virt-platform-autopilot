apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: virt-platform-autopilot-system

namePrefix: virt-platform-autopilot-

resources:
  - ../manager
  - ../rbac

commonLabels:
  app.kubernetes.io/name: virt-platform-autopilot
  app.kubernetes.io/component: operator
